# WI-132: Change legend toggle key from `?` to `/`

## Context
The legend overlay is currently toggled by `?`. This conflicts with the desired `?` key for the new help overlay. Move legend to `/`.

Note: The user mentioned this as changing from `L` to `/`, but the current implementation uses `?` for legend (not `L`). The net result is the same: `/` becomes legend, `?` becomes available for help.

## Existing Code
- `src/hamlet/tui/app.py:99` — `Binding("?", "toggle_legend", "Legend", show=True)` — change to `Binding("/", "toggle_legend", "Legend", show=True)`
- `src/hamlet/tui/legend.py` — `on_key` method handles the key. `render()` shows help text referencing the key.

## Changes

### `src/hamlet/tui/app.py`
```python
# Before:
Binding("?", "toggle_legend", "Legend", show=True),
# After:
Binding("/", "toggle_legend", "Legend", show=True),
```

### `src/hamlet/tui/legend.py`
In `on_key`, change key check from `"question_mark"` to `"slash"`:
```python
# Before:
if event.key == "question_mark":
# After:
if event.key == "slash":
```

In `render()`, update the help text from `"Press ? to toggle"` to `"Press / to toggle"`.

If legend.py has its own BINDINGS tuple referencing `"?"`, update those too.

## Notes
- Textual key names: `"?"` maps to key name `"question_mark"`, `"/"` maps to key name `"slash"`.
- After this change, `?` is free for WI-134 (HelpOverlay).
- `l` is already bound to `scroll_right` (line 93 in app.py) and there is no separate `l` legend binding, so no `l` conflict exists.
