# WI-175 Notes: Remove dead .sh wrapper files from hooks/

## Context
Four `.sh` files exist in `hooks/` but are not referenced by `hooks.json`, `plugin.json`, or any other configuration file. They are dead code.

## Files to delete
- `/Users/dan/code/hamlet/hooks/pre_tool_use.sh`
- `/Users/dan/code/hamlet/hooks/post_tool_use.sh`
- `/Users/dan/code/hamlet/hooks/notification.sh`
- `/Users/dan/code/hamlet/hooks/stop.sh`

## Verification
After deletion:
- `hooks/hooks.json` still references `${CLAUDE_PLUGIN_ROOT}/hooks/pre_tool_use.py` (unchanged)
- No other file references the `.sh` files
- `ls hooks/` shows only `.py` files, `hooks.json`, and `hamlet_hook_utils.py` (after WI-174)
