# WI-173 Notes: hamlet_init improvements and marketplace CLAUDE.md

## Addresses
- SG1 (Cycle 4): hamlet_init success output does not mention server_url — users on non-default ports can't discover customization
- SG2 (Cycle 4): hamlet_init uses Path.cwd() — add optional `path` parameter to make cwd explicit and override-able
- MG1 (Cycle 4): hamlet missing from claude-marketplace CLAUDE.md plugin table

## Files to modify

### 1. `/Users/dan/code/hamlet/mcp/server.py`

**Current tool description:**
```python
description=(
    "Initialize hamlet for the current project. Creates .hamlet/config.json "
    "in the current working directory with a unique project_id and project_name. "
    "Run this once per project before using hamlet."
),
inputSchema={"type": "object", "properties": {}, "required": []}
```

**Desired tool description:**
```python
description=(
    "Initialize hamlet for the current project. Creates .hamlet/config.json "
    "with a unique project_id, project_name, and server_url. "
    "Optionally accepts a path argument to specify the project root "
    "(defaults to current working directory). "
    "Run this once per project before using hamlet."
),
inputSchema={
    "type": "object",
    "properties": {
        "path": {
            "type": "string",
            "description": "Project root directory. Defaults to current working directory."
        }
    },
    "required": []
}
```

**Current success text (line 58-64):**
```python
text=(
    f"Created {config_path}:\n{json.dumps(config, indent=2)}\n\n"
    "Next steps:\n"
    "1. Start the hamlet daemon:  hamlet daemon\n"
    "2. Open the viewer:          hamlet\n"
    "The viewer will show your village as Claude Code agents work."
)
```

**Desired success text:**
```python
text=(
    f"Created {config_path}:\n{json.dumps(config, indent=2)}\n\n"
    "Next steps:\n"
    "1. Start the hamlet daemon:  hamlet daemon\n"
    "2. Open the viewer:          hamlet\n"
    "The viewer will show your village as Claude Code agents work.\n\n"
    "To use a different server address, edit server_url in the config above."
)
```

**Current already-exists text (line 43-46):**
```python
text=f"Hamlet config already exists at {config_path}:\n{json.dumps(existing, indent=2)}\nNo changes made."
```

**Desired already-exists text:**
Add server_url hint at the end.

**Path parameter handling:**
```python
project_root = Path(arguments.get("path", "")).resolve() if arguments.get("path") else Path.cwd()
cwd = project_root
```

### 2. `/Users/dan/code/claude-marketplace/CLAUDE.md`

Add hamlet row to the Plugin Registry table:
```
| hamlet | `github` → `devnill/hamlet` | Visualizes Claude Code agent activity as a roguelike idle game |
```
