# WI-154: Fix event routing dead code in EventProcessor

## Context
`src/hamlet/event_processing/event_processor.py:_route_event` calls:
- `self._world_state.handle_event(event)` — method does not exist
- `self._persistence.log_event(event)` — method does not exist

Due to hasattr checks, these calls silently no-op. Events are processed but not routed to world state or persisted.

## Options
Read event_processor.py and evaluate which approach makes sense:

**Option A**: Implement the missing methods:
- Add `handle_event(event)` to WorldStateManager that processes the event
- Add `log_event(event)` to PersistenceFacade that stores the event

**Option B**: Remove the dead code:
- If event handling happens elsewhere (inference engine), remove the unreachable branches
- Leave a comment explaining where events flow instead

## Notes
- Read event_processor.py, manager.py, and persistence/facade.py before deciding
- Option A is safer for completeness; Option B is safer if the code is truly dead
- Check if events already flow through another path before adding duplicate routing
