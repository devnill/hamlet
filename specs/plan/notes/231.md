# WI-231 Implementation Notes

## src/hamlet/tui/remote_world_state.py

In `_parse_structure`, add `size_tier` to the Structure constructor call:

```python
size_tier=int(d.get("size_tier", 1)),
```

Position it after `work_required` to match the Structure dataclass field order.
Default 1 for backwards compat (daemons running older versions won't send size_tier).

Read the current `_parse_structure` function to find exact location and matching field names.

## Tests in tests/test_remote_world_state.py

1. `_parse_structure({"id": "s1", "village_id": "v1", "type": "house", ..., "size_tier": 3})`
   → Assert result.size_tier == 3

2. `_parse_structure({"id": "s1", "village_id": "v1", "type": "house", ...})` (no size_tier key)
   → Assert result.size_tier == 1
