## WI-125 Incremental Review
### Verdict: FAIL
### Findings

- [CRITICAL] `/Users/dan/code/hamlet/src/hamlet/mcp_server/serializers.py:93-110` — `serialize_state` calls `await world_state.get_all_agents()` and the other `WorldStateManager` methods without a null guard. `MCPServer.__init__` accepts `world_state=None` as a default, and the HTTP routes are registered regardless of whether `world_state` was supplied. If a client hits `GET /hamlet/state` or `GET /hamlet/events` before a `world_state` is injected, `serialize_state(None, ...)` will call `None.get_all_agents()`, raising `AttributeError`. The outer `try/except` in `_handle_state` and `_handle_events` will catch this and return `{"error": ...}` with status 500, so the endpoint does not crash the server, but it returns a 500 instead of the intended state. The fix is to guard at the top of each serializer function: `if world_state is None: return {"agents": [], ...}` (or raise a descriptive error that is caught as a 503). The same null path applies to `serialize_events`.

- [SIGNIFICANT] `/Users/dan/code/hamlet/src/hamlet/mcp_server/serializers.py:102` — `animation_manager._frames` is a `dict[str, int]` mapping agent IDs to raw tick counters. These tick counts are not frame indices; the public API (`get_animation_state`) applies `// TICKS_PER_SPIN_FRAME % 4` or `// TICKS_PER_PULSE_FRAME % 2` to derive a displayable frame. Consumers of `GET /hamlet/state` who read `animation_frames` will receive raw tick counts rather than display frames, with no documentation of the mapping. This makes the field misleading and incompatible with any client that interprets the value as a frame index. If the intent is to expose raw ticks, the field should be named `animation_ticks` or the response should document the conversion. If the intent is to expose display frames, `get_animation_state` must be called per-agent. The direct access of the private `_frames` attribute is also a contract violation that will break if the internal representation changes.

- [SIGNIFICANT] `/Users/dan/code/hamlet/tests/test_mcp_server.py` — There are no tests at all for `serialize_state`, `serialize_events`, `_handle_state`, or `_handle_events`. The existing `test_mcp_server.py` file covers only `__init__`, `start`, and `stop`. Missing test coverage includes: happy-path serialization of each entity type, ISO 8601 formatting of datetime fields, enum `.value` serialization, the `animation_manager=None` branch, the `animation_manager` provided branch, and the exception path returning `{"error": ...}` with status 500. The serializers module has zero test coverage.

- [MINOR] `/Users/dan/code/hamlet/src/hamlet/mcp_server/serializers.py:9-14` — The `_serialize_value` helper is defined but never called. All datetime and enum serialization in the private `_serialize_*` functions is done inline via `.isoformat()` and `.value`. The unused function should be removed to avoid confusion about the intended serialization path.

- [MINOR] `/Users/dan/code/hamlet/src/hamlet/mcp_server/serializers.py:81-90` — `_serialize_event` accesses `entry.hook_type` directly as a string field (confirmed by `EventLogEntry` definition and `handle_event` which stores `event.hook_type.value`). This is correct, but the field is documented in the acceptance criterion as "enum fields as `.value`." Since `hook_type` on `EventLogEntry` is already a `str`, there is no `.value` to call here, but this is a pre-existing design decision in the data model, not a bug introduced by this work item. No action required; noted for clarity.
