id: WI-176
type: work_item
title: Improve mcp import check in start.sh
status: pending
complexity: low
scope: []
depends: []
blocks: []
criteria:
  - The mcp import check in the python3 fallback uses `python3 -c "from mcp.server import Server"` instead of `python3 -c "import mcp"`
  - If the import fails, the existing diagnostic message is still emitted to stderr and exits 1
  - The script remains POSIX sh compatible
  - The python3 availability check (command -v python3) remains unchanged and precedes the import check
module: null
domain: null
notes: |
  # WI-176 Notes: Improve mcp import check in start.sh
  
  ## Context
  The current check `python3 -c "import mcp"` only verifies the top-level mcp namespace is importable. A broken install with just the namespace directory would pass. Changing to `from mcp.server import Server` verifies the specific class used by `mcp/server.py` is available.
  
  ## File to modify
  `/Users/dan/code/hamlet/mcp/start.sh`
  
  ## Current check (line 11)
  ```sh
  if ! python3 -c "import mcp" 2>/dev/null; then
      echo "hamlet: mcp package not found. Install uv (https://docs.astral.sh/uv/) or run: python3 -m pip install mcp" >&2
      exit 1
  fi
  ```
  
  ## Desired check
  ```sh
  if ! python3 -c "from mcp.server import Server" 2>/dev/null; then
      echo "hamlet: mcp package not found. Install uv (https://docs.astral.sh/uv/) or run: python3 -m pip install mcp" >&2
      exit 1
  fi
  ```
  
  Only the import expression changes. The diagnostic message and exit code are unchanged.
cycle_created: 1
cycle_modified: null
content_hash: "1498e1906bb4780d630ca6bee39dd0a11e0190833bdde5e83c08451eb8583533"
token_count: 408
file_path: .ideate/work-items/WI-176.yaml
