id: WI-147
type: work_item
title: Fix StructureUpdater dict unpacking bug
status: pending
complexity: low
scope: []
depends: []
blocks: []
criteria:
  - update_structure called with keyword args not positional dict
  - stage, material, work_units pass as kwargs to world_state.update_structure()
  - Structure stages advance correctly after fix
module: null
domain: null
notes: |
  # WI-147: Fix StructureUpdater dict unpacking bug
  
  ## Context
  `src/hamlet/simulation/structure_updater.py` calls:
  ```python
  await world_state.update_structure(structure.id, {"stage": new_stage, ...})
  ```
  But `update_structure` in manager.py expects `**fields` (keyword args), not a positional dict.
  The dict is treated as a single unknown field name and skipped, so structures never progress.
  
  ## Fix
  Change the call to use keyword args:
  ```python
  await world_state.update_structure(
      structure.id,
      stage=new_stage,
      material=new_material,
      work_units=0,
  )
  ```
  
  ## Notes
  - Read structure_updater.py and manager.py:update_structure before changing
  - Check all call sites of update_structure in the file (may be multiple)
cycle_created: 1
cycle_modified: null
content_hash: d31bf213e70d8395b2e6a63a0b44263bcfc39567a00d59f034fe01c7f81cafa9
token_count: 310
file_path: .ideate/work-items/WI-147.yaml
