id: WI-268
type: work_item
title: Renderer protocol and symbol config extraction
status: done
complexity: medium
scope: []
depends: []
blocks: []
criteria:
  - RendererProtocol is a typing.Protocol with render_frame and cleanup methods
  - SymbolConfig aggregates AgentVisual, StructureVisual, TerrainVisual dataclasses
  - default_config() returns values matching current symbols.py hardcoded values
  - symbols.py delegates to default_config() preserving all public exports
  - All existing tests importing from symbols.py pass without modification
module: null
domain: null
notes: |
  # WI-268: Renderer protocol and symbol config extraction

  ## Implementation Notes

  ### RendererProtocol
  Define in `src/hamlet/gui/renderer_protocol.py` using `typing.Protocol`:
  - `render_frame(viewport_state, agents, structures, terrain_data, event_log)`
  - `cleanup()`

  Check actual types in `hamlet.viewport.coordinates` and `hamlet.world_state.types`.

  ### SymbolConfig extraction
  Create `src/hamlet/gui/symbol_config.py` with frozen dataclasses:
  - `AgentVisual`: symbol ("@"), colors dict (AgentType -> color), zombie_color
  - `StructureVisual`: symbols dict, stage_symbols, material_colors
  - `TerrainVisual`: symbols dict (terrain type -> char), colors dict
  - `SymbolConfig`: agent, structure, terrain

  `default_config()` creates SymbolConfig with exact values from current `symbols.py`.

  ### symbols.py refactor
  Replace module-level dicts with references to `default_config()`. All public exports must continue to work identically. The `get_*` rendering helpers remain in symbols.py.
cycle_created: 1
cycle_modified: null
content_hash: d97aff2c730856824a1a48c76949e92905270ee10a6493b57fd747c7b1b02be7
token_count: 434
file_path: .ideate/work-items/WI-268.yaml
resolution: RendererProtocol in gui/renderer_protocol.py, SymbolConfig in gui/symbol_config.py
