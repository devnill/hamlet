id: WI-139
type: work_item
title: Fix stale legend key tests and update README
status: pending
complexity: small
scope: []
depends: []
blocks: []
criteria:
  - All test assertions referencing "?" for legend changed to "/"
  - All test assertions referencing "question_mark" for legend changed to "slash"
  - README.md legend entry updated from "?" to "/"
  - README.md documents daemon/viewer workflow (hamlet daemon, hamlet view, hamlet init)
module: null
domain: null
notes: |
  # WI-139: Fix stale legend key tests and update README
  
  ## Context
  WI-132 changed legend toggle from `?` to `/`. Tests were not updated. README still references `?` for legend.
  
  ## Existing Code
  - `tests/test_tui_app.py` — lines pressing `"?"` for legend, asserting `"?" in binding_keys` for toggle_legend
  - `tests/test_tui_legend.py` — line asserting `"Press ? to toggle"` and `"question_mark" in binding_keys`
  - `README.md` — line referencing `` `?` - Toggle legend ``
  
  ## Changes
  
  ### `tests/test_tui_app.py`
  - Change all `pilot.press("?")` calls for legend toggle to `pilot.press("/")`
  - Change any `"?" in binding_keys` assertions for toggle_legend to `"/" in binding_keys`
  - Add assertion for `"?" in binding_keys` for toggle_help (if not already present)
  
  ### `tests/test_tui_legend.py`
  - Change `"Press ? to toggle"` assertion to `"Press / to toggle"`
  - Change `"question_mark" in binding_keys` assertion to `"slash" in binding_keys`
  
  ### `README.md`
  - Update legend key reference from `?` to `/`
  - Add daemon/viewer workflow section documenting:
    - `hamlet daemon` — start backend
    - `hamlet view` — open TUI (daemon must be running)
    - `hamlet init` — initialize project config
    - `?` — help overlay in TUI
    - `/` — legend in TUI
  
  ## Notes
  Read each file carefully before making changes — identify all occurrences of the old key references.
cycle_created: 1
cycle_modified: null
content_hash: "50f318b47adb95c319541132c6962d83a83b09c07d97cd0f542ff05631695162"
token_count: 501
file_path: .ideate/work-items/WI-139.yaml
