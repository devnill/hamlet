id: WI-259
type: work_item
title: Add Missing Parameters to Parameter Panel
status: done
complexity: low
scope: []
depends: []
blocks: []
criteria:
  - TERRAIN_PARAMS includes region_scale with appropriate min/max/step
  - TERRAIN_PARAMS includes region_blending with appropriate min/max/step
  - TERRAIN_PARAMS includes river_count with appropriate min/max/step
  - TERRAIN_PARAMS includes pond_count with appropriate min/max/step
  - TERRAIN_PARAMS includes min_pond_size with appropriate min/max/step
  - TERRAIN_PARAMS includes max_pond_size with appropriate min/max/step
  - TERRAIN_PARAMS includes water_percentage_target with appropriate min/max/step
  - TERRAIN_PARAMS includes forest_water_adjacency_bonus with appropriate min/max/step
  - TERRAIN_PARAMS includes forest_region_bias_strength with appropriate min/max/step
  - TERRAIN_PARAMS includes forest_percentage_target with appropriate min/max/step
  - Parameter panel displays all configurable terrain parameters with current values
module: null
domain: null
notes: |
  # WI-259: Add Missing Parameters to Parameter Panel

  ## Objective
  Expose all TerrainConfig parameters in the map viewer parameter panel.

  ## Finding Addressed
  S2: Ten TerrainConfig parameters missing from TERRAIN_PARAMS. This is an unmet acceptance criterion from WI-252.

  ## File Scope
  - `src/hamlet/tui/parameter_panel.py` — TERRAIN_PARAMS list

  ## Implementation Notes
  Add the following parameters to TERRAIN_PARAMS with appropriate min/max/step values:

  ```python
  ("region_scale", "Region Scale", 50.0, 200.0, 10.0, "Biome region size (cells)"),
  ("region_blending", "Region Blend", 0.0, 1.0, 0.1, "Biome transition sharpness"),
  ("river_count", "Rivers", 0, 10, 1, "Number of rivers (0=auto)"),
  ("pond_count", "Ponds", 0, 20, 1, "Number of ponds (0=auto)"),
  ("min_pond_size", "Min Pond", 3, 10, 1, "Minimum pond cells"),
  ("max_pond_size", "Max Pond", 10, 30, 1, "Maximum pond cells"),
  ("water_percentage_target", "Water %", 0.0, 50.0, 1.0, "Target water coverage"),
  ("forest_water_adjacency_bonus", "Water Bonus", 0.0, 1.0, 0.1, "Forest spawn bonus near water"),
  ("forest_region_bias_strength", "Forest Bias", 0.0, 1.0, 0.1, "Regional forest density bias"),
  ("forest_percentage_target", "Forest %", 0.0, 50.0, 1.0, "Target forest coverage"),
  ```

  Reference existing TERRAIN_PARAMS entries for format.

  ## Acceptance Criteria
  - All 10 parameters displayed with labels and descriptions
  - Parameters have appropriate min/max ranges matching TerrainConfig defaults
  - Parameters update the terrain when changed
cycle_created: 1
cycle_modified: null
content_hash: 3463281c591df560de2b17f6a29545303587b113aa302cb6d0b57c6ed7ffad2c
token_count: 671
file_path: .ideate/work-items/WI-259.yaml
resolution: ParameterPanel has all terrain parameters
